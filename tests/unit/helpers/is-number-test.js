import { isNumber } from 'dummy/helpers/is-number';
import { module, test } from 'qunit';

module('Unit | Helper | is number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNumber([42]);
  assert.ok(result);
});
