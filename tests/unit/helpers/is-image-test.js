import { isImage } from 'dummy/helpers/is-image';
import { module, test } from 'qunit';

module('Unit | Helper | is image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isImage(['foo.jpg']);
  assert.ok(result);
});
