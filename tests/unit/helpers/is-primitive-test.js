import { isPrimitive } from 'dummy/helpers/is-primitive';
import { module, test } from 'qunit';

module('Unit | Helper | is primitive');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isPrimitive([42]);
  assert.ok(result);
});
